# Xcode
*.xcodeproj
*.xcworkspace
!*.xcodeproj/project.pbxproj
xcuserdata/
DerivedData/
*.xcresult
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Local, per-developer configuration (signing team etc.)
Config/Local.xcconfig
# Signing material for local releases (private key, certificate, notary API key)
Config/Signing/
# Publisher branding overrides for the App Store build (xcconfig and logo asset catalog)
Config/Branding/

# Coverage and build output
coverage/
build/
dist/
*.profraw
*.profdata

# macOS
.DS_Store

# Secrets: never commit credentials or environment files
.env
.env.*
*.pem
*.p12
*.mobileprovision
*.provisionprofile

# Claude Code agent worktrees and local session files
.claude/
